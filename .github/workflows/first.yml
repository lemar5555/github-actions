name: first
on: [push]

jobs:
  job1:
    name: First Job
    runs-on: ubuntu-latest
    steps:
      - name: Step One
        uses: actions/checkout@v2
      - name: Step Two
        run:  env | sort
  job2:
    name: Second Job
    runs-on: ubuntu-latest
    steps:
      - name: Step One
        uses: actions/checkout@v2
      - name: Step Two
        run:  env | sort
  job3:
    name: Third Job
    runs-on: ubuntu-latest
    needs: [job1, job2]
    steps:
      - name: Step One
        run: echo "Hello"
